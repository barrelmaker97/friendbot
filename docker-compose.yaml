version: "3.7"
services:
  app:
    image: "barrelmaker97/friendbot:latest"
    ports:
      - "5000:5000"
    volumes:
      - type: bind
        source: ${FB_EXPORT}
        target: /export
        read_only: true
    restart: always
  redis:
    image: "redis:alpine"
    restart: always
